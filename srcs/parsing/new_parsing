지금 상황

$ 문자를 환경변수, 리턴 값으로 써야하는지 문자로 써야하는지에 대해
현재 알고리즘으로는 해결할 수없음
-> quote 로 감싼 것을 없애주기 떄문에 이게 그냥 문자인지 값인지 모름
-> 모든 입력값을 파싱하고 리스트로 생상한 뒤에 명령어들일 실행하기 때문

-> 일단 현재 변수를 나눈 상태까지 됐음

지금

[ 이전의 쓰레기같은 알고리즘]

파싱 전부 -> 명령어실행


[ 새로운 파싱 알고리즘 ]
gnl => 
1. 입력 : 명령어 인자 인자 | 명령어 인자 인자 | 명령어 인자 인자 
1

2. 처리
str = 명령어 인자 인자
-> str_to_env
-> parsing.c 플레그저장

 명령어 인자 인자 | 명령어
-> str_to_env
-> parsing.c 플레그저장

명령어
-> str_to_env
-> parsing.c 플레그저장

[ 명령어 실행 ]
1. 명령어들이 어떻게 실행되어야하나?
    프로세스를 새로 만들어야하는 경우
    빌트인 명령어일 경우
2. 파이프, 라다이렉션 같은 경우 프로세스를 따로 생성해줘야하나?

< 해야하는거 >
[ 파싱 ]
1. 끊어서 파싱하고 실행하는 것
2. $ 같이 리턴값이 필요한 경우 명령어 실행 과정에 대한 지식필요할듯?

토요일 전까지

명령어 실행 1, 2 번부분 공부하고 공유
주말에 카톡이나 온라인으로 파싱부분 역할 분담하고 구현해놓기
